---
interface Actividad {
  titulo: string;
  descripcion: string;
  horario: string;
  lugar: string;
  categoria: string;
  icono: string;
  color: string;
  destacado?: boolean;
}

interface Categoria {
  nombre: string;
  icono: string;
  color: string;
}

const actividades: Actividad[] = [
  {
    titulo: "Culto Dominical",
    descripcion: "Nuestro servicio principal de adoración y predicación.",
    horario: "Domingos 10:00 AM",
    lugar: "Templo Principal",
    categoria: "Cultos",
    icono: "church",
    color: "bg-blue-100 text-blue-800",
    destacado: true
  },
  {
    titulo: "Estudio Bíblico",
    descripcion: "Profundiza en la Palabra de Dios con nuestro grupo de estudio.",
    horario: "Miércoles 7:00 PM",
    lugar: "Sala de Estudio",
    categoria: "Estudios",
    icono: "menu_book",
    color: "bg-purple-100 text-purple-800",
    destacado: true
  },
  {
    titulo: "Grupo de Jóvenes",
    descripcion: "Encuentros semanales para jóvenes con actividades y enseñanza.",
    horario: "Sábados 4:00 PM",
    lugar: "Salón de Jóvenes",
    categoria: "Jóvenes",
    icono: "groups",
    color: "bg-green-100 text-green-800",
    destacado: true
  },
  {
    titulo: "Escuela Dominical",
    descripcion: "Clases bíblicas para todas las edades.",
    horario: "Domingos 9:00 AM",
    lugar: "Aulas de Clase",
    categoria: "Educación",
    icono: "school",
    color: "bg-yellow-100 text-yellow-800"
  },
  {
    titulo: "Grupo de Oración",
    descripcion: "Momento de intercesión y comunión con Dios.",
    horario: "Martes 6:00 PM",
    lugar: "Capilla de Oración",
    categoria: "Oración",
    icono: "volunteer_activism",
    color: "bg-red-100 text-red-800"
  },
  {
    titulo: "Ensamble Musical",
    descripcion: "Práctica del grupo de alabanza y adoración.",
    horario: "Viernes 7:00 PM",
    lugar: "Salón de Música",
    categoria: "Música",
    icono: "music_note",
    color: "bg-indigo-100 text-indigo-800"
  }
];

---

<section class="relative bg-gradient-to-br from-slate-50 to-blue-50 py-20">
  <!-- Patrón de fondo sutil -->
  <div class="absolute inset-0 bg-[linear-gradient(45deg,_transparent_25%,_rgba(59,130,246,0.03)_25%,_rgba(59,130,246,0.03)_50%,_transparent_50%,_transparent_75%,_rgba(59,130,246,0.03)_75%)] bg-[length:20px_20px]"></div>
  
  <div class="container mx-auto px-6 relative z-10">
    <div class="max-w-7xl mx-auto">
      <!-- Encabezado -->
      <div class="text-center mb-16">
        <h1 class="text-4xl md:text-5xl font-bold text-gray-900 mb-4">Nuestras Actividades</h1>
        <p class="text-xl text-gray-600 max-w-2xl mx-auto">
          Descubre todas las actividades y eventos que tenemos para ti y tu familia.
        </p>
      </div>

      <!-- Search and Filter Section -->
      <div class="mb-12 flex flex-col md:flex-row gap-6 items-center justify-between">
        <div class="w-full md:w-96">
          <div class="relative">
            <input
              type="text"
              placeholder="Buscar actividades..."
              class="w-full px-4 py-3 pl-12 rounded-xl border border-gray-300 focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
            />
            <span class="material-icons absolute left-4 top-1/2 -translate-y-1/2 text-gray-400">search</span>
          </div>
        </div>
      </div>

      <!-- Actividades Destacadas -->
      <div class="mb-16">
        <h2 class="text-2xl font-bold text-gray-900 mb-8">Actividades Destacadas</h2>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
          {actividades.filter(a => a.destacado).map((actividad) => (
            <div class="bg-white rounded-3xl p-8 shadow-xl hover:shadow-2xl transition-shadow duration-300">
              <div class="flex items-center gap-4 mb-6">
                <div class={`w-16 h-16 rounded-full ${actividad.color} flex items-center justify-center`}>
                  <span class="material-icons text-3xl">{actividad.icono}</span>
                </div>
                <div>
                  <h3 class="text-xl font-bold text-gray-900">{actividad.titulo}</h3>
                  <p class="text-gray-600">{actividad.categoria}</p>
                </div>
              </div>
              <p class="text-gray-600 mb-6">{actividad.descripcion}</p>
              <div class="space-y-3">
                <div class="flex items-center gap-2 text-gray-600">
                  <span class="material-icons text-gray-400">schedule</span>
                  <span>{actividad.horario}</span>
                </div>
                <div class="flex items-center gap-2 text-gray-600">
                  <span class="material-icons text-gray-400">location_on</span>
                  <span>{actividad.lugar}</span>
                </div>
              </div>
            </div>
          ))}
        </div>
      </div>

      <!-- Todas las Actividades -->
      <div class="mb-16">
        <h2 class="text-2xl font-bold text-gray-900 mb-8">Todas las Actividades</h2>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
          {actividades.map((actividad) => (
            <div class="bg-white rounded-3xl p-8 shadow-xl hover:shadow-2xl transition-shadow duration-300">
              <div class="flex items-center gap-4 mb-6">
                <div class={`w-16 h-16 rounded-full ${actividad.color} flex items-center justify-center`}>
                  <span class="material-icons text-3xl">{actividad.icono}</span>
                </div>
                <div>
                  <h3 class="text-xl font-bold text-gray-900">{actividad.titulo}</h3>
                  <p class="text-gray-600">{actividad.categoria}</p>
                </div>
              </div>
              <p class="text-gray-600 mb-6">{actividad.descripcion}</p>
              <div class="space-y-3">
                <div class="flex items-center gap-2 text-gray-600">
                  <span class="material-icons text-gray-400">schedule</span>
                  <span>{actividad.horario}</span>
                </div>
                <div class="flex items-center gap-2 text-gray-600">
                  <span class="material-icons text-gray-400">location_on</span>
                  <span>{actividad.lugar}</span>
                </div>
              </div>
            </div>
          ))}
        </div>
      </div>

      <!-- Call to Action -->
      <div class="bg-white rounded-3xl p-12 shadow-xl">
        <div class="max-w-3xl mx-auto text-center">
          <h2 class="text-3xl font-bold text-gray-900 mb-6">¿Quieres Participar?</h2>
          <p class="text-gray-600 mb-8">
            Únete a nuestras actividades y forma parte de nuestra comunidad. ¡Te esperamos!
          </p>
          <button class="px-8 py-4 bg-blue-600 text-white rounded-xl hover:bg-blue-700 transition-colors duration-300">
            Contactar Organización
          </button>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  /* Animación suave para los elementos */
  .flex {
    animation: fadeIn 0.5s ease-out;
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(10px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>